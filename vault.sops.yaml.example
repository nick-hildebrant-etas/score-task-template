# vault.sops.yaml.example
#
# Fill in your values, then encrypt and use:
#   cp vault.sops.yaml.example vault.sops.yaml
#   task secrets:edit
#
# Never commit vault.sops.yaml in plaintext — the pre-commit hook will block it.
# Only vault.sops.yaml.example (this file) is committed unencrypted.
#
postgres_password: "changeme"
nexus_admin_password: "changeme"
nexus_docker_port: "5000"
github_org: "myorg"
runner_token: "ghp_yourtoken"
